Compilation Notes
=================

* Install liveScript

    npm install -g livescript

* Compile the .ls file to JavaScript

    livescript -c sieve.ls

* Run the compiled source

    node sieve.js
